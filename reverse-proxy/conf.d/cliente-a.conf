server {
    listen 80;

    server_name cliente-a.local;

    location / {
        proxy_pass http://cliente-a_grafana:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
