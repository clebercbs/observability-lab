global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:

  - job_name: "prometheus-self"
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: "clientes-dinamicos"
    file_sd_configs:
      - files:
          - /etc/prometheus/clients/*.yml
